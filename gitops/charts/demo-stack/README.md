# Legacy Bootstrap Chart

This Helm chart is kept only as a bootstrap compatibility path for `cloud-init`.

The live `demo-stack` Argo CD application has been migrated to Kustomize under:

- `gitops/kustomize/demo-stack`

Do not add new live deployment changes here unless you are intentionally updating
the bootstrap compatibility path as well.
