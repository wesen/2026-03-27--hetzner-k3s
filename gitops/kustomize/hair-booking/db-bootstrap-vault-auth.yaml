apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: hair-booking-db-bootstrap
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  vaultConnectionRef: vault
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: hair-booking-db-bootstrap
    serviceAccount: hair-booking-db-bootstrap
