apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: pretext-trace-ghcr-pull
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  vaultAuthRef: pretext-trace
  mount: kv
  type: kv-v2
  path: apps/pretext-trace/prod/image-pull
  refreshAfter: 30s
  destination:
    name: pretext-trace-ghcr-pull
    create: true
    overwrite: true
    type: kubernetes.io/dockerconfigjson
    transformation:
      excludes:
        - ".*"
      templates:
        .dockerconfigjson:
          text: |
            {"auths":{"{{ .Secrets.server }}":{"username":"{{ .Secrets.username }}","password":"{{ .Secrets.password }}","auth":"{{ .Secrets.auth }}"}}}
