apiVersion: batch/v1
kind: Job
metadata:
  name: smailnail-db-bootstrap
  annotations:
    argocd.argoproj.io/sync-wave: "1"
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation,HookSucceeded
spec:
  backoffLimit: 6
  template:
    metadata:
      labels:
        app.kubernetes.io/name: smailnail-db-bootstrap
        app.kubernetes.io/component: bootstrap
    spec:
      enableServiceLinks: false
      restartPolicy: OnFailure
      serviceAccountName: smailnail-db-bootstrap
      containers:
        - name: bootstrap
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - /bootstrap/bootstrap.sh
          env:
            - name: POSTGRES_SERVICE_HOST
              valueFrom:
                secretKeyRef:
                  name: smailnail-postgres-admin
                  key: service-host
            - name: POSTGRES_SERVICE_PORT
              valueFrom:
                secretKeyRef:
                  name: smailnail-postgres-admin
                  key: service-port
            - name: POSTGRES_ADMIN_USER
              valueFrom:
                secretKeyRef:
                  name: smailnail-postgres-admin
                  key: postgres-user
            - name: POSTGRES_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smailnail-postgres-admin
                  key: postgres-password
            - name: SMAILNAIL_DB_NAME
              valueFrom:
                secretKeyRef:
                  name: smailnail-runtime
                  key: database
            - name: SMAILNAIL_DB_USER
              valueFrom:
                secretKeyRef:
                  name: smailnail-runtime
                  key: username
            - name: SMAILNAIL_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smailnail-runtime
                  key: password
          volumeMounts:
            - name: bootstrap-script
              mountPath: /bootstrap
              readOnly: true
      volumes:
        - name: bootstrap-script
          configMap:
            name: smailnail-db-bootstrap-script
            defaultMode: 0555
